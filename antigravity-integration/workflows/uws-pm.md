---
description: Manage Issues and Project Board
---

# UWS Project Management

Manage tickets and the project board.

**Arguments:**
- `command`: `create`, `list`, `move`, `board`
- `args`: Arguments for the command (e.g. title, ID, status)

```bash
bash ./scripts/pm.sh "$1" "$2" "$3" "$4"
```
