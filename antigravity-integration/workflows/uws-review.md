---
description: Review and Approve/Reject Change Lists
---

# UWS Review Workflow

Manage the Approval Process.

**Arguments:**
- `command`: `list`, `approve`, `reject`
- `cl_id`: The Change Request ID (CR-XXX)

```bash
bash ./scripts/review.sh "$1" "$2"
```
