# MCP Server Environment Variables
# Copy this to .env.mcp and fill in your values

# ============================================
# RESEARCH TOOLS
# ============================================

# Semantic Scholar API (free tier available)
# Get key at: https://www.semanticscholar.org/product/api
SEMANTIC_SCHOLAR_API_KEY=your_semantic_scholar_api_key

# Zotero Integration
# Get credentials at: https://www.zotero.org/settings/keys
ZOTERO_API_KEY=your_zotero_api_key
ZOTERO_USER_ID=your_zotero_user_id

# ============================================
# DATABASE TOOLS
# ============================================

# PostgreSQL Connection
# Format: postgresql://user:password@host:port/database
POSTGRES_CONNECTION_STRING=postgresql://localhost/mydb

# ============================================
# EXPERIMENT TRACKING
# ============================================

# MLflow Tracking Server
MLFLOW_TRACKING_URI=http://localhost:5000

# ============================================
# DEVOPS / MONITORING
# ============================================

# Prometheus Server URL
PROMETHEUS_URL=http://localhost:9090

# ============================================
# PREREQUISITES (Install these first)
# ============================================

# Node.js & npm (for npx commands)
# Python & uv (for uvx commands)
#   pip install uv

# Docker (for terraform, prometheus servers)
#   sudo apt install docker.io

# Pandoc (for document conversion)
#   sudo apt install pandoc texlive-xetex

# Vale (for prose linting)
#   brew install vale  OR  snap install vale

# kubectl & helm (for kubernetes server)
#   Required if using kubernetes MCP server
